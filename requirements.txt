# Зафиксированы совместимые диапазоны версий, чтобы сборка была
# воспроизводимой и не ломалась на минорных обновлениях библиотек.
# Перед деплоем рекомендуется свериться с актуальными версиями
# на PyPI — сеть в текущем окружении аудита была недоступна для
# автоматической проверки.
pyTelegramBotAPI>=4.14,<5.0
google-generativeai>=0.5,<1.0

# openai удалён из зависимостей: ни один модуль проекта его не
# импортирует (было мёртвой зависимостью). Если планируете
# использовать OpenAI как fallback-провайдера (см. Phase 2
# миграции — core/ai_engine/openai_provider.py), верните эту
# строку обратно.
